-1.compute.amazonaws.com
56.10.8.0

cd ~/Downloads
ls *.pem
chmod 400 xau-signal-key.pem
ssh -i ./xau-signal-key.pem ubuntu@56.10.8.0

sudo apt update
sudo apt install -y docker.io git
sudo systemctl enable --now docker

git clone https://github.com/ThangNV2k2/XAU.git xau-signal
cd xau-signal

git pull
sudo docker build -t xau-signal 

sudo docker rm -f xau-signal
sudo docker run -d \
  --name xau-signal \
  --restart unless-stopped \
  --memory 450m \
  --memory-swap 450m \
  --env-file .env \
  -v "$PWD/logs:/app/logs" \
  --log-driver local \
  --log-opt max-size=10m \
  --log-opt max-file=1 \
  --log-opt compress=false \
  xau-signal
  
  sudo docker ps
sudo docker logs -f --tail 100 xau-signal